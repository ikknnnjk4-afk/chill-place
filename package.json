{
  "name": "le-couloir",
  "version": "1.0.0",
  "private": true,
  "description": "Le Couloir, jeu d'horreur psychologique hors-ligne pour Android.",
  "scripts": {
    "prepare-web": "rm -rf www && mkdir -p www && cp -R index.html css js assets www/",
    "android:sync": "npm run prepare-web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  }
}