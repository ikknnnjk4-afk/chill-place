{
  "name": "le-couloir",
  "version": "1.0.0",
  "description": "LE COULOIR – jeu d'horreur psychologique mobile",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
